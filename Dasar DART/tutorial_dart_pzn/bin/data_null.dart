void main() {
  /// di dalam dart ada yang namanya data null atau kosong

  // String data;
  // print(data); // ini akan eror karena datanya belum di isi, untuk bisa melakukan itu kita bisa menggunkan

  String? data;
  print(data);

  //di tipe data yang dynamic kita tidak perlu ? karena akan otomatis menjadi null, contohnya seperti var
}

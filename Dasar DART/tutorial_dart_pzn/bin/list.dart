void main() {
  ///list atau array adalah tempat untuk menyimpan data yang bisa di akses menggunkan index(dimulai dari 0)
  //cara membuat list
  /**
   * list<tipeData> namaVariable = [];
   * var namaVariabel = <tipeData> [];
   */

  List<int> nilai = []; //list kosong
  var nama = <String>[];

  print(nilai);
  print(nama);

  //bisa juga langsung di masukan datanya
  var kota = <String>['sampit', 'bandung', 'tokyo', 'jakarta'];
  print(kota);
}

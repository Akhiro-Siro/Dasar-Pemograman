void main() {
  //set hampir sama dengan list hanya saja ada sedikit perbedaan
  //set tidak memiliki index dan nilai yang di simpan unik tidak bisa sama

  //set <tipeData> namaVariabel = {};
  // var namaVariable = <tipeData> [];
  // var namaVariable = <tipeData> {};

  Set<String> nama = {};
  var nilai = <int>{};

  print(nilai);
  print(nama);

  var kota = <String>{'banjar', 'sampit', 'jakarta'};
  print(kota);
}
